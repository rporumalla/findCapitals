var assert = require('assert');
var fs = require("fs");
var app = require('../app');

describe('Find Capitals', function() {
  describe('Positive Test Cases', function() {
    it('should return capitals for country code and name', function(){
        // TO DO
        // app.getCapitalCity();
        // var path = "../out/output.txt";
        // this.timeout(20000);
        // var text = fs.readFileSync(path);
        // var textByLine = text.split("\n")
        // assert.equal(['Diego Garcia', 'Apia', 'Jakarta', 'New Delhi', 'Port Moresby', 'New Delhi'], textByLine);
    });
  });
});